name: AI Development

on:
  push:
    paths:
      - "requirements/ai/**"

jobs:
  ai-development:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check requirements
        run: |
          ls -la requirements/ai